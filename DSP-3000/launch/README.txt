To have the DSP-3000 device node launch automatically at statup, copy this file into 
your /core.d directory.  If you "flavoured" the DSP-3000 interface cable then the launch
file will require no modification.  Otherwise, you will need to modify the path to the 
interface cable.
